# Phase 5 Decision Fixtures

These inputs contain normalized `[0,1]` factors for standalone Decision Engine validation only. They are not measurements from the Phase 2 benchmark or Phase 3 monitor. Missing factors must remain unavailable; they must not be interpreted as zero.
